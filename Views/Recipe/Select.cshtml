@{
    ViewData["Title"] = "Recipes";
}

<div class="text-center">
    @*<form asp-action="" method="get" class="form-inline mb-3"> * need to know which model to pull from for general search*@
    <h2 class="display-4">Select Recipe</h2>
    <div class="form-group">
        <input type="text" class="form-control" placeholder="Search Recipes..." name="searchTerm" value="@ViewData["CurrentFilter"]" />
    </div>
    <button type="submit" class="btn btn-primary ml-2">Search</button>
    @*</form>*@
</div>
<div class="row">
    @*@if (Model!=null) *A loop for when we set up with model*
    {
        foreach (var recipe in Model)
        {*@
            <div class="col-md-4">
                <div class="card mb-4">
                    <div class="card-body">
                        <h5 class="card-title">Recipe1 name</h5> @*@recipe.Name*@
                        <p class="card-text">Recipe1 Description</p> @*@recipe.Description*@
                        @*need to tell model which recipe*@
                        <a asp-controller="Cookbooks" asp-action="Add">Select</a>
                    </div>
                </div>
            </div>
        @*}
    }
    else
    {
        <p>No Recipes Found.</p>
    }*@
</div>